import 'package:flutter/material.dart';

class AddressWorkingScreen extends StatelessWidget {
  const AddressWorkingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Column(children: [
        
      ],),
    );
  }
}
